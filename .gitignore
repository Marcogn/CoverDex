# Gradle
.gradle/
build/
app/build/

# Local configuration
local.properties

# Android Studio / IntelliJ
.idea/
*.iml
.DS_Store
captures/
.externalNativeBuild/
.cxx/

# Keystores (never commit a signing key)
*.keystore
*.jks

# Logs
*.log

# legacy-web — the parked React/Capacitor PWA (see legacy-web/README.md).
# Deleted in Phase 6; ignored the same way it was as the repo root until then.
legacy-web/node_modules
legacy-web/dist
legacy-web/dist-android
legacy-web/dist-ssr
legacy-web/coverage
legacy-web/*.local
legacy-web/.vite
legacy-web/.env
legacy-web/.env.*
!legacy-web/.env.example
legacy-web/public/icons/icon-192x192.png
legacy-web/public/icons/icon-512x512.png
legacy-web/public/icons/apple-touch-icon.png
legacy-web/assets/
